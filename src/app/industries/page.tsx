import { ComingNext } from "@/components/layout/ComingNext";

export const metadata = { title: "Industries" };

export default function Page() {
  return <ComingNext title="Industries" description="Sector pages linking through to the equipment each type of work calls for." />;
}
